apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: terraform-google-datalake
  title: Terraform module for GCP data lake
  description: >
    This Terraform module allows you to configure and deploy a data lake with multiple buckets, lifecycle rules and other features.
  labels:
    technical: "1"
  annotations:
    github.com/project-slug: artefactory/terraform-google-datalake
    backstage.io/techdocs-ref: dir:.
  tags:
    - infrastructure
    - terraform
    - datalake
    - module
    - gcp
spec:
  type: service
  owner: user:pe-artefact
  lifecycle: experimental
